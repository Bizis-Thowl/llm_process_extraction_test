### This config file contains required core defaults that must be set, along with a handful of common optional settings.
### For a full list of available settings, see https://microsoft.github.io/graphrag/config/yaml/

### LLM settings ###
## There are a number of settings to tune the threading and token limits for LLM calls - check the docs.

completion_models:
  default_completion_model:
    model_provider: openai #vllm #ollama
    model: Qwen3-30B-A3B-Instruct #qwen3_8b_40k
    auth_method: api_key # or azure_managed_identity
    api_key: ${GRAPHRAG_API_KEY} # set this in the generated .env file, or remove if managed identity
    api_base: http://hal9000.skim.th-owl.de:11877/v1
    retry:
      type: exponential_backoff

embedding_models:
  default_embedding_model:
    model_provider: ollama
    model: qwen3_emb_8b_40k
    auth_method: api_key
    api_key: ${GRAPHRAG_API_KEY}
    api_base: http://hal9000.skim.th-owl.de:11976
    retry:
      type: exponential_backoff

### Document processing settings ###

input:
  type: text # [csv, text, json, jsonl]

chunking:
  type: tokens
  size: 1200
  overlap: 100
  encoding_model: o200k_base

### Storage settings ###
## If blob storage is specified in the following four sections,
## connection_string and container_name must be provided

input_storage:
  type: file # [file, blob, cosmosdb]
  base_dir: "input"

output_storage:
  type: file # [file, blob, cosmosdb]
  base_dir: "output"

reporting:
  type: file # [file, blob]
  base_dir: "logs"

cache:
  type: json # [json, memory, none]
  storage:
    type: file # [file, blob, cosmosdb]
    base_dir: "cache"

#vector_store:
#  default:
#    type: lancedb
#    db_uri: ./output/lancedb
#  entity_description:
#    vector_size: 4096   # set to match your embedding model
#  community_full_content:
#    vector_size: 4096
#  text_unit_text:
#    vector_size: 4096

vector_store:
  type: lancedb
  db_uri: output/lancedb
  vector_size: 4096
  index_schema:
    community_full_content:
      index_name: community_full_content
      id_field: id
      vector_field: vector
      vector_size: 4096
    entity_description:
      index_name: entity_description
      id_field: id
      vector_field: vector
      vector_size: 4096
    text_unit_text:
      index_name: text_unit_text
      id_field: id
      vector_field: vector
      vector_size: 4096
  text_unit_text:
    vector_size: 4096
  entity_description:
    vector_size: 4096
#vector_store:
#  type: lancedb
#  db_uri: output\lancedb

### Workflow settings ###

embed_text:
  embedding_model_id: default_embedding_model

extract_graph:
  completion_model_id: default_completion_model
  prompt: "prompts/extract_graph_deutsch.txt"
  entity_types: [organization,person,geo,event]
  max_gleanings: 1

summarize_descriptions:
  completion_model_id: default_completion_model
  prompt: "prompts/summarize_descriptions_deutsch.txt"
  max_length: 500

extract_graph_nlp:
  text_analyzer:
    extractor_type: regex_english # [regex_english, syntactic_parser, cfg]

cluster_graph:
  max_cluster_size: 10

extract_claims:
  enabled: false
  completion_model_id: default_completion_model
  prompt: "prompts/extract_claims_deutsch.txt"
  description: "Any claims or facts that could be relevant to information discovery."
  max_gleanings: 1

community_reports:
  completion_model_id: default_completion_model
  graph_prompt: "prompts/community_report_graph_deutsch.txt"
  text_prompt: "prompts/community_report_text_deutsch.txt"
  max_length: 2000
  max_input_length: 8000

snapshots:
  graphml: false
  embeddings: false

### Query settings ###
## The prompt locations are required here, but each search method has a number of optional knobs that can be tuned.
## See the config docs: https://microsoft.github.io/graphrag/config/yaml/#query

local_search:
  completion_model_id: default_completion_model
  embedding_model_id: default_embedding_model
  prompt: "prompts/local_search_system_prompt_deutsch.txt"

global_search:
  completion_model_id: default_completion_model
  map_prompt: "prompts/global_search_map_system_prompt_deutsch.txt"
  reduce_prompt: "prompts/global_search_reduce_system_prompt_deutsch.txt"
  knowledge_prompt: "prompts/global_search_knowledge_system_prompt_deutsch.txt"

drift_search:
  completion_model_id: default_completion_model
  embedding_model_id: default_embedding_model
  prompt: "prompts/drift_search_system_prompt_deutsch.txt"
  reduce_prompt: "prompts/drift_search_reduce_prompt_deutsch.txt"

basic_search:
  completion_model_id: default_completion_model
  embedding_model_id: default_embedding_model
  prompt: "prompts/basic_search_system_prompt_deutsch.txt"
